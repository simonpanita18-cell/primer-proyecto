# ============================================================
# 🐳 ENTORNO DOCKER - envio
# ============================================================

# Redirección interna hacia el contenedor de MySQL en el puerto nativo 3306
# (Usa db_envio si así se crea en tu init-db.sql)
spring.datasource.url=jdbc:mysql://mysql:3306/db_envio?useSSL=false&serverTimezone=America/Santiago&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=rootpass

# Redirección interna hacia el contenedor de Eureka
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
# Ajustes de red dinámicos para Docker
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}