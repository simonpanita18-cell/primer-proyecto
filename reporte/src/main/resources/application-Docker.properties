spring.application.name=reporte
server.port=8089

# Base de Datos (Docker MySQL)
spring.datasource.url=jdbc:mysql://mysql:3306/db_reporte?useSSL=false&serverTimezone=America/Santiago&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=rootpass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Eureka
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=reporte-service
eureka.instance.instance-id=${spring.application.name}:${server.port}
